import { PageShell } from "@/components/page-shell";

export default function RoomsPage() {
  return (
    <PageShell
      title="Rooms"
      description="Room management page shell is ready. Room status and tenancy assignment will be added later."
    />
  );
}
