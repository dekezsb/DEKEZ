import { PageShell } from "@/components/page-shell";

export default function TenantsPage() {
  return (
    <PageShell
      title="Tenants"
      description="Tenant management page shell is ready. Tenant records and portal access will be added later."
    />
  );
}
