import { PageShell } from "@/components/page-shell";

export default function PropertiesPage() {
  return (
    <PageShell
      title="Properties"
      description="Property management page shell is ready. Property records will connect to Supabase in the next phase."
    />
  );
}
