# Preserve existing DEKEZ requirements

New requests are additive unless the user explicitly replaces an earlier rule. Do not remove existing features to deliver another feature.

TA policy confirmed 2026-09-22: prepare a missing unsigned next term within 30 days of expiry, including expired unsigned predecessors; retain the actual prior six- or twelve-month duration, asking management for non-standard durations. Current/future unsigned TA rent follows the latest confirmed rent; signed history is immutable. Never record tenant consent or send messages merely because a standby offer is prepared. Existing terms must not be duplicated. Historical signatures use the actual signing timestamp and must not roll current tenancy dates, rent or deposit backward. Keep the current-tenant never-signed list with all versions, date-based expiry and overlap warnings, and keep tests/ta-followup.test.cjs in the release gate. Identity verification authorized on 2026-09-22 was a one-time approval of existing current-tenant IC/passport files, not permission to approve future uploads automatically.

Before a release, retrieve current production/main source and compare it with the working branch. Never publish a stale standalone worktree over newer production changes. Preserve both histories through reviewed integration. Run `npm run test:release-regressions` and TypeScript checks; do not delete or bypass the prebuild tests to make a release pass.

TA follow-up confirmation 2026-09-22: management chose six-month next terms for INS1, INS4, KLB1, KLB13 and KLB15. Preserve their prepared terms; no duplicate generation. The 09:00 Codex follow-up was cancelled at management's request. Maintain the portal's existing authenticated renewal maintenance and live lists instead; do not recreate a Codex reminder.

Standing requirements:
- Keep the editable bank transaction reference/code in every admin verification confirmation, including booking fees and Rent + Deposit. Save it to the existing submission/payment and use exact reference tokens in matching. Preserve leading zeroes, existing values on blank/old/rejection forms, room/month constraints and duplicate guards. Keep `tests/verification-bank-reference.test.cjs` in the mandatory release gate. Booking reference saving and allocation must succeed or roll back together.
- Keep reconciliation confidence filters (All, Exact / High, Possible, Manual Review, Unmatched), counts, search and highest-confidence-first ordering.
- A bank property/room reference must constrain the actual payment dropdown to that property, room and bank month, not merely add a warning. Show the room's same-month invoices (including paid invoices) on the row. Never fall back to all tenants when no exact-room record exists: when the bank line's own reference/description has no property+room code (expanded 2026-09-22), the dropdown must still narrow to the same month AND to payments `rankExistingPayments` already identifies by tenant name or an exact bank-reference-token match — never every same-month tenant, and never a payment identified only by date/amount proximity. A bare room number with no property code must never be guessed at as a property/room hint: bank accounts/statements carry no property linkage in this system's data model (a single statement can span multiple properties), so inferring property from a digit alone risks misdirecting money to the wrong tenant. Keep `tests/bank-room-scope.test.cjs` in the release gate.
- Completed reconciliation items leave the working queue. Completed records remain in the ledger, not a duplicate history page. Hide completed statements only when both money-in and money-out work is done.
- Statement buttons, selection and pending-statement counts must use remaining bank-line work, not the import's `in_progress` status or balance difference. A finished statement must not reappear from an old URL. Keep all statement records/balances for accounting and keep `tests/statement-work-queue.test.cjs` in the mandatory release gate.
- Keep existing tenant uploads, receipts, payment statuses and outstanding logic unchanged during accounting-only work. Existing verified payments remain the master records; never duplicate receipts, payments or AR postings.
- Keep all chart account types available in every payment voucher line, including assets and liabilities.
- Preserve reservation navigation, monthly check-in/check-out navigation, single-slip booking registration and protected booking-fee allocation.
- Preserve current-tenancy deposit scoping; historical tenant accounting records must remain intact.
- Retain the requested daily imports/deduplication, bank reference entry, QR room/month invoice matching, paid-invoice reconciliation and audited ledger corrections when integrating their implementations. Do not claim any is present merely because an older branch contained it.
- Reconciliation is allocation against ALREADY verified payments, never invoice verification. Use payment remaining = verified amount minus linked amount and bank remaining = bank amount minus allocated amount. Allocate MIN of those balances, never require bank/payment/invoice totals to equal or invoice outstanding to be positive. Support one bank to multiple payments, one payment to multiple banks, and grouped portions of one verified slip. Partial banks/payments stay available; only exhausted balances leave the queue. Preserve duplicate, room/month, permission and audit protections. Keep `tests/payment-allocation.test.cjs` and `tests/payment-allocation-sql.test.cjs` in the mandatory release gate. Auto-queue this allocation for bulk reconcile (user-approved 2026-09-22, relaxed 2026-09-22, expanded to name/reference identity 2026-09-22) whenever exactly one available confirmed payment is identified for that bank line and month — identified either by a property+room code (no conflict) matching that room, or, when the line has no room code, by a tenant-name or exact bank-reference-token match that `rankExistingPayments` already recognizes (the same "identified" candidate set — not `missingIdentity` — that the manual dropdown now uses; date/amount proximity alone never counts as identification) — the gate is exactly `canAllocateExistingPayment` (the same eligibility/duplicate/used/scope/positive-amount check the per-row Reconcile button itself uses), never a stricter check on top of it. A bank amount that differs from the payment amount is NOT grounds for exclusion or "Manual Review" — it is an ordinary partial payment, and MIN-of-balances applies whatever the amount is; a missing slip/receipt on the existing payment is likewise not grounds for exclusion. When more than one confirmed payment is identified for the SAME bank line (user-approved 2026-09-22: a bank line may legitimately identify a tenant's deposit and a separate rent invoice paid together, and forcing that into manual review for two individual clicks is not the correct concept — one bulk action should cover it), the bulk queue still includes the line, as a multi-payment allocation, whenever `canBulkGroupPayments` confirms every identified candidate is unambiguous: the same tenant identity (prefer `tenancyId`; otherwise same tenant name AND same property/room) AND no two candidates share the exact same amount (which would make them indistinguishable duplicates of one another — that case is excluded and stays manual, same as the existing "duplicate receipts never become ready" rule). The bank line is then allocated to each identified payment in turn via the same `reconcileExistingPayment` RPC, called once per payment in sequence against the same bank line — the exact "one bank to multiple payments" shape already described above, and identical to an admin picking one payment, reconciling, then picking the next; the server recomputes the bank line's live remaining balance before each call, so no client-side coordination beyond calling them in order is required or assumed. Only more than one identified candidate for DIFFERENT tenants, an equal-amount pair among the candidates, a duplicate/used/conflict flag, or an unidentifiable room (for the direct-invoice fallback, which still requires a property+room code) keeps a line out of bulk. A bare room number with no property code is never treated as identification, for the same data-model reason given above. When multiple bank lines in one bulk run target the same payment or invoice, the bulk preview table must track running consumption per payment/invoice id (in the order shown) so "already paid"/"remaining" figures reflect what earlier lines in that same run already applied; the actual reconciliation still runs each line sequentially through the same server-side RPC, which recomputes live balances itself and is already safe from double-allocation regardless of the preview. The bulk action still calls the same `reconcileExistingPayment` allocation used by the per-row Reconcile button, once per bank line, with the same MIN-of-balances and duplicate/eligibility checks; it never skips or batches those checks.
- Keep one room/month invoice containing the existing rent, deposit, electricity top-up and other charge lines. Reconciliation must never create a second invoice, payment, receipt, charge or AR posting, reopen a paid invoice, or alter tenant outstanding. The one narrow exception is the direct bank-to-invoice match described below; outside that exact case this rule stands unchanged.
- Direct bank-to-invoice match (narrow exception, user-approved 2026-09-21): when a bank statement line's own reference/description contains exactly one recognizable property+room code (no conflicting codes), that code matches exactly one still-open room-month rental invoice with no existing verified payment or confirmed slip covering it, and the bank amount does not exceed that invoice's outstanding balance, an admin may reconcile the bank line straight to that invoice via the existing `apply_bank_to_room_invoice` database function. This creates exactly one confirmed payment (tagged with its originating bank line via `origin_bank_line_id`) and updates that invoice's paid amount/status — this is the only place reconciliation is allowed to create a payment or change invoice status. Never use it when a verified payment or pending/verified slip already exists for that room/month (allocate to the existing payment instead, per the rule above), and never on an ambiguous, conflicting, locked, or already-matched bank line — the database function itself enforces all of these and must not be weakened. Keep the existing audit logging (`rent_bill_audit_logs`, `accounting_audit_logs`) for every such action. This same match may also be offered in bulk (a "Reconcile all N now" action): it must only group bank lines that individually meet every condition above, including partial-amount ones, and must call the same per-line database function and audit log once per bank line — never a batch shortcut that skips a line's own checks.
- The single "Reconcile all N now" bulk action combines both auto-queued cases above — existing-payment allocation and direct bank-to-invoice — into one list (user-approved 2026-09-22), so the admin reviews one set of confident, month-identified matches (by room, name, or exact reference) and fires them together. A bank line is auto-queued into at most one of the two cases, never both, and only when the identity evidence is unambiguous; anything with more than one candidate, a duplicate/conflict flag, or an amount that exceeds what's available stays out of the bulk list for manual matching in the table below.

Release reports must distinguish tested/restored features from outstanding integration work. No real financial submissions during UI verification.

One verified parent slip may have existing allocations to different invoices. Group by the verified submission, not invoice equality; display all linked invoice allocations. Preserve existing tenant/room/month and duplicate protections. Do not restructure invoices or create new ones to reconcile such a group.
